# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# AndroidX settings
android.enableJetifier=true
android.useAndroidX=true
android.defaults.buildfeatures.buildconfig=true
android.bundle.enableUncompressedNativeLibs=false

# Note: NDK ABI filters should be set in build.gradle defaultConfig block
# The config plugin will handle this automatically

# Note: ABI filters are also configured via expo-build-properties in app.config.ts
# This ensures only arm64-v8a libraries are compiled and included in the APK

